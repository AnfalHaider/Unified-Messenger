<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="UnifiedMessenger.Controls.CommandCenterPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="2">
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Command center" />
                <TextBlock
                    x:Name="SubtitleText"
                    FontSize="12"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="10"
                VerticalAlignment="Center">
                <CalendarDatePicker
                    x:Name="FromDatePicker"
                    MinWidth="130"
                    DateChanged="OnCustomDateChanged"
                    PlaceholderText="From"
                    Visibility="Collapsed" />
                <CalendarDatePicker
                    x:Name="ToDatePicker"
                    MinWidth="130"
                    DateChanged="OnCustomDateChanged"
                    PlaceholderText="To"
                    Visibility="Collapsed" />
                <ComboBox
                    x:Name="WindowSelector"
                    MinWidth="110"
                    SelectionChanged="OnWindowChanged"
                    ToolTipService.ToolTip="Caught-up % is measured over conversations active in this date range">
                    <ComboBoxItem Content="Today" IsSelected="True" Tag="Today" />
                    <ComboBoxItem Content="Last 7 days" Tag="Week" />
                    <ComboBoxItem Content="All time" Tag="All" />
                    <ComboBoxItem Content="Custom range" Tag="Custom" />
                </ComboBox>
                <Border
                    CornerRadius="6"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Padding="2">
                    <StackPanel Orientation="Horizontal" Spacing="2">
                        <ToggleButton
                            x:Name="GroupByAccountButton"
                            Content="By account"
                            IsChecked="True"
                            Click="OnGroupByAccountClick"
                            Padding="10,4"
                            CornerRadius="4" />
                        <ToggleButton
                            x:Name="GroupByLocationButton"
                            Content="By location"
                            IsChecked="False"
                            Click="OnGroupByLocationClick"
                            Padding="10,4"
                            CornerRadius="4" />
                    </StackPanel>
                </Border>
                <Button
                    x:Name="ResyncButton"
                    Click="OnResyncHistory"
                    Content="Re-sync history"
                    ToolTipService.ToolTip="Re-read history from each account and reconcile answered conversations" />
                <Button Click="OnRefresh" Content="Refresh" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <AutoSuggestBox
                x:Name="SearchBox"
                MaxWidth="360"
                HorizontalAlignment="Left"
                PlaceholderText="Filter accounts or locations…"
                QueryIcon="Find"
                TextChanged="OnSearchChanged" />
            <ToggleSwitch
                x:Name="DensityToggle"
                Grid.Column="1"
                MinWidth="0"
                OffContent="Comfortable"
                OnContent="Compact"
                Toggled="OnDensityToggled"
                ToolTipService.ToolTip="Denser rows for many accounts" />
        </Grid>

        <Border
            x:Name="LocationCtaBanner"
            Grid.Row="2"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="14,10"
            Visibility="Collapsed">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="13">
                    <Run>Group accounts into </Run><Run FontWeight="SemiBold">locations</Run><Run> to oversee a whole branch at once.</Run>
                </TextBlock>
                <Button
                    x:Name="DefineLocationsButton"
                    Grid.Column="1"
                    Content="Define locations ↗"
                    Click="OnDefineLocations"
                    VerticalAlignment="Center" />
            </Grid>
        </Border>

        <Border
            x:Name="AttentionBanner"
            Grid.Row="3"
            Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
            CornerRadius="8"
            Padding="12,10"
            Visibility="Collapsed">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="AttentionText" VerticalAlignment="Center" TextWrapping="WrapWholeWords" />
                <Button
                    x:Name="AttentionJumpButton"
                    Grid.Column="1"
                    Content="Jump"
                    Click="OnAttentionJump"
                    Visibility="Collapsed"
                    VerticalAlignment="Center" />
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="CardsHost" Spacing="8" />
        </ScrollViewer>
    </Grid>
</UserControl>
