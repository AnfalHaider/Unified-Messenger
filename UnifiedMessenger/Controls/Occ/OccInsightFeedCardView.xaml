<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="UnifiedMessenger.Controls.Occ.OccInsightFeedCardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:UnifiedMessenger.Controls.Occ"
    xmlns:shared="using:UnifiedMessenger.Controls.Shared"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:DefaultBindMode="OneWay"
    mc:Ignorable="d">

    <shared:SurfaceCard Margin="0,0,0,8">
        <shared:SurfaceCard.CardBody>
            <StackPanel Spacing="4">
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock FontWeight="SemiBold" Text="{x:Bind ViewModel.CustomerName}" TextTrimming="CharacterEllipsis" />
                    <TextBlock Grid.Column="1" FontSize="10" Opacity="0.55" Text="{x:Bind ViewModel.SourceLabel}" />
                </Grid>
                <TextBlock FontSize="11" Opacity="0.65" Text="{x:Bind ViewModel.BranchName}" />
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <Border Padding="6,2" Background="{ThemeResource LayerFillColorAltBrush}" CornerRadius="4" Visibility="{x:Bind ViewModel.IntentLabelVisibility}">
                        <TextBlock FontSize="10" Text="{x:Bind ViewModel.IntentLabel}" />
                    </Border>
                    <Border Padding="6,2" Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="4" Visibility="{x:Bind ViewModel.UrgencyLabelVisibility}">
                        <TextBlock FontSize="10" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" Text="{x:Bind ViewModel.UrgencyLabel}" />
                    </Border>
                </StackPanel>
                <TextBlock FontSize="12" Text="{x:Bind ViewModel.Summary}" TextWrapping="WrapWholeWords" />
            </StackPanel>
        </shared:SurfaceCard.CardBody>
    </shared:SurfaceCard>
</UserControl>
