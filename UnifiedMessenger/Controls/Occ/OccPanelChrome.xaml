<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="UnifiedMessenger.Controls.Occ.OccPanelChrome"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowSpacing="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
            x:Name="ChromeHeader"
            ColumnSpacing="8"
            Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <FontIcon
                x:Name="DragGripIcon"
                FontFamily="Segoe MDL2 Assets"
                FontSize="12"
                Glyph="&#xE700;"
                Opacity="0.55" />
            <TextBlock
                Grid.Column="1"
                FontSize="11"
                FontWeight="SemiBold"
                Opacity="0.75"
                Text="{x:Bind PanelTitle, Mode=OneWay}" />
            <Button
                x:Name="HidePanelButton"
                Grid.Column="2"
                Click="HidePanelButton_Click"
                Content="Hide"
                Style="{StaticResource SubtleButtonStyle}" />
            <StackPanel
                Grid.Column="3"
                Orientation="Horizontal"
                Spacing="4">
                <Button
                    x:Name="ShrinkWidthButton"
                    Click="ShrinkWidthButton_Click"
                    Content="-"
                    Style="{StaticResource SubtleButtonStyle}" />
                <Button
                    x:Name="GrowWidthButton"
                    Click="GrowWidthButton_Click"
                    Content="+"
                    Style="{StaticResource SubtleButtonStyle}" />
            </StackPanel>
        </Grid>

        <ContentPresenter
            Grid.Row="1"
            x:Name="PanelContentPresenter"
            Content="{x:Bind PanelContent, Mode=OneWay}" />
    </Grid>
</UserControl>
