<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="UnifiedMessenger.Controls.Shared.MetricCardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border MinWidth="90" Padding="12,8" Style="{StaticResource DashboardMetricCardStyle}">
        <StackPanel Spacing="4">
            <TextBlock
                FontSize="11"
                Opacity="0.65"
                Text="{x:Bind Label, Mode=OneWay}" />
            <TextBlock
                FontSize="20"
                FontWeight="SemiBold"
                Foreground="{StaticResource UmBrandBlueBrush}"
                Text="{x:Bind Value, Mode=OneWay}" />
            <TextBlock
                FontSize="10"
                Opacity="0.55"
                Text="{x:Bind Subtext, Mode=OneWay}"
                Visibility="{x:Bind SubtextVisibility, Mode=OneWay}" />
        </StackPanel>
    </Border>
</UserControl>
