<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="UnifiedMessenger.Controls.Shared.OperationsThreadCardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border
        Padding="{x:Bind CardPadding, Mode=OneWay}"
        Background="{ThemeResource LayerFillColorDefaultBrush}"
        BorderBrush="{x:Bind ViewModel.CardBorderBrush, Mode=OneWay}"
        BorderThickness="{x:Bind ViewModel.CardBorderThickness, Mode=OneWay}"
        CornerRadius="8"
        ToolTipService.ToolTip="{x:Bind ViewModel.NextActionSummary, Mode=OneWay}">
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border
                Background="{x:Bind ViewModel.CardBorderBrush, Mode=OneWay}"
                CornerRadius="2"
                Visibility="{x:Bind SlaStripeVisibility, Mode=OneWay}" />
            <StackPanel Grid.Column="1" Spacing="{x:Bind ContentSpacing, Mode=OneWay}">
                <Grid ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <FontIcon
                        FontSize="{x:Bind PlatformIconSize, Mode=OneWay}"
                        Glyph="{x:Bind ViewModel.PlatformGlyph, Mode=OneWay}"
                        Opacity="0.75" />
                    <TextBlock
                        Grid.Column="1"
                        FontSize="{x:Bind CustomerNameFontSize, Mode=OneWay}"
                        FontWeight="SemiBold"
                        Text="{x:Bind ViewModel.CustomerName, Mode=OneWay}"
                        TextTrimming="CharacterEllipsis" />
                    <Border
                        Grid.Column="2"
                        Padding="{x:Bind UrgencyBadgePadding, Mode=OneWay}"
                        Background="{x:Bind ViewModel.UrgencyBrush, Mode=OneWay}"
                        CornerRadius="4">
                        <TextBlock
                            FontSize="{x:Bind UrgencyBadgeFontSize, Mode=OneWay}"
                            Foreground="White"
                            Text="{x:Bind ViewModel.UrgencyLabel, Mode=OneWay}" />
                    </Border>
                </Grid>
                <TextBlock
                    FontSize="10"
                    Opacity="0.55"
                    Text="{x:Bind ViewModel.InboxLabel, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis" />
                <TextBlock
                    FontSize="10"
                    Opacity="0.45"
                    Text="{x:Bind ViewModel.BranchName, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis"
                    Visibility="{x:Bind ViewModel.BranchNameVisibility, Mode=OneWay}" />
                <StackPanel Orientation="Horizontal" Spacing="6" Visibility="{x:Bind TagRowVisibility, Mode=OneWay}">
                    <Border Padding="6,2" Background="{ThemeResource LayerFillColorAltBrush}" CornerRadius="4">
                        <TextBlock FontSize="10" Text="{x:Bind ViewModel.IntentLabel, Mode=OneWay}" />
                    </Border>
                    <Border Padding="6,2" Background="{x:Bind ViewModel.SentimentBrush, Mode=OneWay}" CornerRadius="4">
                        <TextBlock FontSize="10" Foreground="White" Text="{x:Bind ViewModel.SentimentLabel, Mode=OneWay}" />
                    </Border>
                </StackPanel>
                <TextBlock
                    FontSize="{x:Bind SummaryFontSize, Mode=OneWay}"
                    FontWeight="{x:Bind SummaryFontWeight, Mode=OneWay}"
                    MaxLines="{x:Bind SummaryMaxLines, Mode=OneWay}"
                    Opacity="0.85"
                    Text="{x:Bind ViewModel.NextActionSummary, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="WrapWholeWords" />
                <TextBlock
                    FontSize="10"
                    FontWeight="SemiBold"
                    Foreground="{x:Bind ViewModel.SlaBrush, Mode=OneWay}"
                    Text="{x:Bind ViewModel.SlaText, Mode=OneWay}"
                    Visibility="{x:Bind ViewModel.SlaVisibility, Mode=OneWay}" />
                <TextBlock
                    FontSize="10"
                    Opacity="0.65"
                    Text="{x:Bind ViewModel.RevenueDisplay, Mode=OneWay}"
                    Visibility="{x:Bind ViewModel.RevenueVisibility, Mode=OneWay}" />
                <TextBlock
                    FontSize="10"
                    Opacity="0.45"
                    Text="Click to open thread"
                    Visibility="{x:Bind ClickHintVisibility, Mode=OneWay}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
