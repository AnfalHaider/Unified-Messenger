<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="UnifiedMessenger.Controls.WeeklyActivityChart"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock FontWeight="SemiBold" Text="7-day activity" />
            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="12">
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <Border
                        Width="10"
                        Height="10"
                        Background="{ThemeResource AccentFillColorDefaultBrush}"
                        CornerRadius="2" />
                    <TextBlock FontSize="11" Opacity="0.65" Text="Sent" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <Border
                        Width="10"
                        Height="10"
                        Background="{ThemeResource SystemFillColorSuccessBrush}"
                        CornerRadius="2" />
                    <TextBlock FontSize="11" Opacity="0.65" Text="Received" />
                </StackPanel>
            </StackPanel>
        </Grid>

        <TextBlock
            x:Name="SummaryText"
            FontSize="11"
            Opacity="0.65"
            Text="No activity recorded yet." />

        <Grid x:Name="ChartHost" MinHeight="120" ColumnSpacing="6" />

        <TextBlock
            x:Name="EmptyHint"
            HorizontalAlignment="Center"
            Opacity="0.55"
            Text="Activity will appear as messages are tracked."
            Visibility="Collapsed" />
    </StackPanel>
</UserControl>
