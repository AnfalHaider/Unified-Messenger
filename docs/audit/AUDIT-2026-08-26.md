# Full-product audit — 2026-08-26

**Baseline:** `main` @ `98493e5`, v4.99.45 · **Supersedes** `docs/full-product-audit-prompt.md`
(that prompt's wave structure is not followed here; its §7 channel-completeness rule is **closed** — see §4).

> ## Remediation status — v4.99.46, branch `audit/2026-08-26-fixes`
>
> **All eight S1s are closed**, plus S2-12 (partial), S3-02 and S4-01. Increments 66–69.
> Tests 1744 → 1782, all green, zero build warnings.
>
> | ID | What it was | Closed by | Verified |
> |---|---|---|---|
> | S1-01 | Toasts never worked; five Settings controls governed nothing | Inc. 68 | Live — installed build logs the fallback channel engaging; Windows reports the stamped AUMID as `Enabled` |
> | S1-02 | Backup restore silently reverted on close | Inc. 66 | Code trace; flush suppressed, app closes itself |
> | S1-03 | Close could hang forever holding the single-instance mutex | Inc. 67 | Code trace; shutdown moved to the cancellable `Closing` pass, bounded net on `Closed` |
> | S1-04 | Process terminated on a Windows theme/accent change | Inc. 66 | Live — reproduced from `app.log` before the fix |
> | S1-05 | Reviews showed a ≤8 sample as the unanswered total | Inc. 67 | Unit-tested (`ReviewQueueSampleTests`) |
> | S1-06 | Two different "SLA met" figures on one screen | Inc. 67 | Unit-tested (`AnalyticsSlaLabellingTests`) |
> | S1-07 | One conversation dropped from every scan | Inc. 66 | Live — measured 1-in-894 before the fix; unit-tested after |
> | S1-08 | README's AI privacy claim was false | Inc. 69 | Unit-tested (`ReadmePrivacyClaimTests`) |
> | S2-14 | SLA donut relabelled to what it measures | Inc. 67 | Unit-tested |
> | S3-02 | `ShowAlertToast` had no availability guard | Inc. 68 | — |
> | S4-01 | CS8604, the tree's only build warning | Inc. 66 | Build at 0 warnings |
>
> ### Phase 3 also closed (increments 70–71)
>
> | ID | What it was | Verified |
> |---|---|---|
> | S2-02 | **94** failure paths (not 33 — that was one lane's scope) reported only via `Debug.WriteLine`, absent from every shipping build | Live — the new log immediately exposed three defects nobody knew about, including one of my own |
> | S2-04 | `TriagePersistenceService` never migrated to `CorruptFileRecovery`; triage state lost silently | Code trace |
> | S2-05 | Backups captured stale data — no flush before zipping | Code trace |
> | S2-08 | `Probe · …: Object reference not set to an instance of an object.` on the dashboard | Code trace |
> | S2-09 | Save/Export failed silently on four surfaces | Code trace |
> | S2-12 | Two `async void` crash vectors in the browse tab | Code trace |
> | S2-13 | Viewing a past range overwrote *today's* KPI history slot | Code trace |
> | S2-15 | One number under two nouns; an all-time busiest day inside a period report | Code trace |
> | S2-16 | Missed-call count over-stated on the fallback reader, with nothing saying so | Unit-tested |
> | S3-03 | A locked backup reported as "not a recognised backup" | Code trace |
> | S3-13 | "Answered today" zeroed by a date range | Unit-tested |
> | S3-14 | Sparklines plotted readings weeks apart as adjacent | Unit-tested |
>
> **A defect I introduced, and then caught.** The increment-68 fallback copied `Tag` and `Group` onto the
> classic `ToastNotification` unconditionally. `ShowInfoToast` never sets a group, and
> `ToastNotification.Group = null` throws `ArgumentException: Value does not fall within the expected
> range` — so the channel opened, reported itself healthy, and then failed on every delivery. It stayed
> invisible until the S2-02 sweep replaced the `Debug.WriteLine` in the surrounding catch, at which point
> the live log showed three failures per launch. Fixed in increment 71, confirmed by the same log staying
> quiet over the same window. **The lesson is the one this audit keeps re-learning:** a health check that
> reports the *channel* rather than the *delivery* is a reassuring number backed by missing data.
>
> ### New finding — not in the original audit
>
> | # | Finding | Evidence | Severity |
> |---|---|---|---|
> | **S1-09** | **The taskbar badge never works either, and Settings offers a toggle for it.** Both paths fail on every launch: the Windows App SDK badge API (same unpackaged/self-contained root cause as S1-01) and the classic `ITaskbarList3` fallback. | Live log, every launch: `[WRN] [Notifications.Badge] BadgeNotificationManager clear failed: A method was called at an unexpected time.` and `[WRN] [Notifications.Badge] Taskbar overlay fallback failed: Unable to cast COM object … to interface type 'ITaskbarList3' … E_NOINTERFACE`. Sites: `Services/Notifications/TaskbarBadgeService.cs:74,87`, `Services/Notifications/TaskbarOverlayService.cs`. UI: `Pages/SettingsPage.xaml` `TaskbarBadgeToggle`. | S1 |
>
> Found only because of the S2-02 sweep — before it, both failures were `Debug.WriteLine` and invisible in
> Release. The likely fix is symmetrical to S1-01 (`BadgeUpdateManager.CreateBadgeUpdaterForApplication`
> against the same AUMID); the `ITaskbarList3` failure is a separate COM/apartment problem. **Not
> attempted** — a new S1 found after the plan was approved, and it deserves its own increment.
>
> **Not attempted yet:** S2-01, S2-03, S2-06, S2-17, the S3/S4 tail, and doc corrections D-04…D-22
> (deliberately last, so they describe the tree as it then is).
>
> **Residuals, recorded rather than hidden:** on the classic toast channel a click does not open the app,
> because unpackaged activation needs a registered COM activator — Settings says so. The app's own toasts
> are confirmed only by the *absence* of delivery errors over a window in which the broken build logged
> three; toasts fired directly at the same AUMID from outside the app did render on screen.

## Ground truth established this session

| Check | Result |
|---|---|
| `dotnet build UnifiedMessenger.csproj -c Release` | **Succeeds**, 1 warning (CS8604, `ReviewAskCandidates.cs:128`) |
| `dotnet build` at repo root / `UnifiedMessenger.sln` | **FAILS** — `NETSDK1032`, see S2-01 |
| `dotnet test` full suite | **1744 passed, 0 failed, 0 skipped, 24s** |
| Line coverage | **37.9 % (9 872 / 26 074)**, `TestResults/…/coverage.cobertura.xml` dated 2026-06-11; CI floor is 25 % (`build.yml:18`) |
| Version lockstep | **Correct** at 4.99.45 across `csproj:29-31`, `app.manifest:4`, `installer-shared.iss:8`, `README.md:10`, `phase-status.md:3` |
| Installed binary provenance | Built from `.claude/worktrees/project-skills-plugins-cf86f5`, which is at `98493e5` with **zero diff vs `main`** — the machine fact is correct; only the embedded source paths differ (S4-05) |
| Live run of installed v4.99.45 | Launched once from outside the MSIX container; `app.log` copied out via `Invoke-CimMethod`. Produced three of the S1s below. |

### Hard constraints — verified, not assumed

| Constraint | Verdict | Evidence |
|---|---|---|
| No cloud / no APIs / no recurring cost | **Holds** | Only 3 `HttpClient` instances exist: `OllamaInferenceClient.cs:43` (loopback), `OllamaRuntimeService.cs:41` (GitHub release download), `GitHubUpdateService.cs:31` (api.github.com). No other socket/network type in the tree. |
| No telemetry / analytics | **Holds** | No telemetry package in `UnifiedMessenger.csproj:88-97`. No egress carries derived data. |
| App never auto-sends | **Holds** | Every `.click()` in the injected scripts drives navigation or expansion only — `adapter-core.js:214`, `whatsapp-adapter.js:1550` (focus a chat row), `GoogleReviewSnapshotService.cs:70,127,158` (rows-per-page, "More", next page). No Send/Submit control is ever clicked. |
| Oversight data never reaches a browse tab | **Holds** | Per-account isolated `ProfileName` in one shared environment (`WebViewProfileManager.cs:124`); nothing pushes metrics into a WebView — the scrapers only read. |
| All AI on-device | **Holds in practice, unenforced in code** — see S3-09 |
| No unofficial protocol libraries | **Holds** | Package list is clean. |
| Google = reviews + Q&A only | **Holds** | No message metrics anywhere on the Google path. |
| No `eval` on scraped strings | **Holds** | Zero `eval(` / `new Function(` / `innerHTML =` in `Assets/Scripts/*.js`. |
| Injected CSS harms no accessibility | **Holds** | `whatsapp-chrome.css` is 13 lines and sets one border radius. |

---

## 1. Severity table

### S1 — broken, or wrong data shown to the owner as fact

| # | Finding | Evidence | Fix | Effort | Risk |
|---|---|---|---|---|---|
| **S1-01** | **Toast notifications have never worked in the shipping build.** Every proactive-alert surface is dead, and Settings offers five controls that govern nothing. | Live log: `[WRN] [Notifications] Toast registration failed: COMException: The specified module could not be found. / Unable to load resource dll. Microsoft.WindowsAppRuntime.Insights.Resource.dll` (2026-08-26 17:27:56Z). Throw site `Services/Notifications/AppNotificationService.cs:40`; log `:48`. Cause: unpackaged + `WindowsAppSDKSelfContained` (`csproj:19,41`) omits the WindowsAppRuntime **Singleton** package that hosts `PushNotificationsLongRunningTask.exe`; the publish dir ships only `PushNotificationsLongRunningTask.ProxyStub.dll`, and `Microsoft.WindowsAppRuntime.Insights.Resource.dll` is absent from `bin/x64/…/publish` entirely. Nothing calls `SetCurrentProcessExplicitAppUserModelID`, and `installer.iss:45-46` writes shortcuts with no `System.AppUserModel.ID`. Dead call sites: `Services/Oversight/OversightAlertMonitor.cs:127`, `Services/Oversight/GoogleReviewSnapshotService.cs:590`, `Services/Shell/ShellController.cs:309`, `MainWindow.xaml.cs:536`. Dead UI: `Pages/SettingsPage.xaml:54,93,115,132,149`. | Drop `AppNotificationManager` for the unpackaged case: set an explicit AUMID at startup, stamp the same AUMID + toast-activator CLSID onto the Start Menu shortcut from the installer, toast via `ToastNotificationManager.CreateToastNotifier(aumid)` (this is what `MASTER-PLAN.md:§17` actually researched). Then surface the health state in Settings → Notifications instead of one `[WRN]` line. | L | med |
| **S1-02** | **Restoring a backup is silently undone when the app closes.** The recovery path destroys itself. | `Services/LocalBackupService.cs:98` overwrites the JSON stores on disk while all ten stores are still live in memory holding pre-restore state. The caller shows *"Restored N file(s). Please restart…"* and returns without forcing an exit (`Pages/SettingsPage.Data.partial.cs:139-142`). On normal close, `Services/ApplicationLifecycleService.cs:112` `FlushPersistentStateAsync` serialises the in-memory state back over every restored file. The method's own doc comment at `LocalBackupService.cs:96` says *"The caller must restart the app"* — the caller does not. | After a successful restore, suppress the flush (`Interlocked.Exchange(ref _shutdownStarted, 1)`) and restart the process from the app rather than asking. | S | low |
| **S1-03** | **Closing the window can hang the process forever, holding the single-instance mutex** — after which every relaunch exits silently with no window. | `MainWindow.xaml.cs:432` (`Window.Closed`, UI thread) → `Services/ApplicationLifecycleService.cs:30` `ShutdownAsync().GetAwaiter().GetResult()` blocks the UI thread. `ShutdownAsync` uses `ConfigureAwait(false)` throughout (`:56-71`, `:72`), so after the first genuinely-async await it resumes on a thread-pool thread; it then calls `CloseAllSessionsAsync` (`:78`) → `Services/Session/InstanceSessionManager.cs:315` → `Services/UiThreadRunner.cs:72` where `HasThreadAccess` is false → `TryEnqueue` (`:78`) → `await tcs.Task` (`:87`). The UI thread cannot pump, so the callback never runs. Tray-quit is **safe** (`MainWindow.xaml.cs:308` awaits and pre-sets `_forceShutdown`, so `ApplicationLifecycleService.cs:42` short-circuits). Exposed path: the window **X** with `RunInBackgroundOnClose == false`. | Cancel the close, `await ShutdownAsync()`, then `Close()` with `_forceShutdown` set — the shape tray-quit already uses. | S | med |
| **S1-04** | **The app terminates when Windows changes theme or accent colour.** Default `ThemePreference` is System, so this is a normal-use crash. | Live log, 2026-08-26 16:30:01Z: `[ERR] [App.UnhandledException] COMException (0x8001010E)` — `RPC_E_WRONGTHREAD` — at `Microsoft.UI.Xaml.Window.get_Content()` ← `Services/ThemeService.cs:60` ← `Services/ThemeService.cs:189` `OnSystemColorValuesChanged`. `UISettings.ColorValuesChanged` raises on a background thread; `Apply()` dereferences `App.CurrentWindow?.Content` with no marshalling. `App.xaml.cs:90` deliberately leaves `Handled = false`, so the process dies. The same file warns about exactly this at `ThemeService.cs:50-51`. | Marshal `Apply` through `UiThreadRunner`/`DispatcherQueue.TryEnqueue` in `OnSystemColorValuesChanged` and in the high-contrast watcher (`ThemeService.cs:140`). | S | low |
| **S1-05** | **The Reviews page presents a truncated sample as a total, and disagrees with its own sidebar badge.** | The scrape builds previews for only the first eight Reply buttons per page — `Services/Oversight/GoogleReviewSnapshotService.cs:204` `replyBtns.slice(0,8)` — while recording the real count separately as `unanswered: reply` (`:206`). The Reviews hero "Needs a reply" (`Controls/ReviewDesk.xaml.cs:339`) and the "Unanswered" KPI tile (`:491`) both render `_queue.Count`, i.e. ≤8. The reply-rate basis line on the same screen reads "N of M read" from the real counts (`:547`), and the sidebar badge uses `health.Unanswered` (`GoogleReviewSnapshotService.cs:529`). So the badge can say 45 while the page says 8. No caveat is rendered. The comment at `GoogleReviewSnapshotService.cs:155` claims the cap is 24; the effective cap is 8. | Render `Health.Unanswered` as the tile/hero value and label the queue as "showing the N most urgent of M". Fix the stale comment. | M | low |
| **S1-06** | **Two different numbers labelled "SLA met" render on one Analytics screen.** | KPI card = `statNow.SlaCompliancePercent`, denominator is FRT samples (`Services/Analytics/AnalyticsPagePresenter.cs:105`). Donut centre = `sla.Met * 100 / sla.Total` where `Total = Met + Missed + NoSla` (`Pages/AnalyticsPage.xaml.cs:205-206`, `Services/Analytics/ChartModels.cs:56`), so untimeable accounts drag it down. They can differ arbitrarily. `:206` also uses plain `Math.Round`, not `MetricMath.HonestPercent` — a site the v4.99.x rounding fix missed, so it can print 100 % beside a visible "SLA missed" slice. | Give the donut its own label ("Coverage of threads"), and route `:206` through `MetricMath.HonestPercent`. | S | low |
| **S1-07** | **One customer conversation is silently dropped from every scan.** | Live log, repeating on every pass: `[WRN] [ChatEntryParser] Skipped 1 of 894 conversation rows as unparseable: InvalidOperationException: Cannot read incomplete UTF-16 JSON text as string with missing low surrogate.` Catch at `Services/Oversight/ChatEntryParser.cs:69-78`. Root cause is upstream: preview truncation slices UTF-16 code units and can cut an emoji surrogate pair in half — `Assets/Scripts/whatsapp-adapter.js:1075` `.slice(0, 90)`, `:1108` `.slice(0, 100)`, `Assets/Scripts/whatsapp-store-bridge.js:709` `.slice(0, 120)`, `Assets/Scripts/adapter-core.js:151` `.slice(0, 48)`, `:751` `.slice(0, 157)`. The same class exists in C# at `Services/Ai/TranscriptBuilder.cs:20-22`. | One shared `umTruncate(s, n)` that backs off a trailing lone high surrogate (0xD800–0xDBFF); apply at all five JS sites and the C# one. Log-only is not enough — a dropped row must reach the "unreadable chats" hint that `CommandCenterPanel.xaml.cs:1975` already renders. | S | low |
| **S1-08** | **The README makes a privacy claim about AI prompts that is false as written.** The invariant itself holds — nothing leaves the box — but the statement a buyer reads does not describe the product. | `README.md:78`: *"Prompts contain **aggregate counts only** — never customer names or message text."* `Services/Ai/TranscriptBuilder.cs:23-27` emits `Customer: {name}` and `Message: {up to 800 chars}`. `AGENTS.md:206-211` records this exception explicitly and warns against simplifying it away; `SettingsPage.xaml:468` states it correctly. | Replace with the two-path wording (insight strips = aggregates; triage = name + ≤800 chars, to localhost only). Exact text in §3. | S | low |

### S2 — the owner gets stuck, misled, or has to guess

| # | Finding | Evidence | Fix | Effort | Risk |
|---|---|---|---|---|---|
| **S2-01** | `dotnet build` at the repo root fails out of the box. | `UnifiedMessenger.sln:25-26` maps `Release\|Any CPU` → `Release\|x86` for the app project, incompatible with its `win-x64` RID → `NETSDK1032`. Only the csproj-targeted command in `AGENTS.md:47` works. | Map `Any CPU` → `x64` in the `.sln`. | S | low |
| **S2-02** | 33 catch blocks across the ingest, session, backfill and AI paths report only via `Debug.WriteLine`, which the compiler strips from Release. | `Services/Session/WebMessageIngressService.cs:108,145` (the single funnel for **all** scraped traffic), `Services/Adapters/PlatformAdapters.cs:148,165,169,231,267,326,697`, `Services/Backfill/BackfillSyncManager.cs:166,206`, `Services/Ai/AiInferenceQueue.cs:155,214`, `OllamaInferenceClient.cs:70,135,220`, `OllamaRuntimeService.cs:98,146,225,330,421`, `Services/Session/InstanceSessionManager.cs:365,404,431,452,634,666`, `WebViewProfileManager.cs:75`, `Services/Shell/ShellController.cs:231,1026,1065`, `Services/Notifications/TaskbarBadgeService.cs:74,87`. `AppNotificationService.cs:45-47` documents this exact trap; the fix was applied at that one site only. | Replace with `AppLogger`; rate-limit the ingest one ("dropped N of M web messages"). | M | low |
| **S2-03** | The `ConditionalWeakTable<CoreWebView2, …>` bug class AGENTS.md documents still appears in four places. | `Services/Adapters/PlatformAdapters.cs:27` (dedupe guard — a dropped entry re-injects all three scripts), `Services/Adapters/PlatformNavigationHooks.cs:13` (`Detach` silently no-ops → orphan `NavigationCompleted` handler survives session dispose → double `NavigationRetryScheduler` calls), `Services/Session/WebViewChromeStyleInjector.cs:18`, `Services/Session/WebViewNavigationGuard.cs:46` (**decision path is correctly closure-captured** — the Custom-URL blanking bug has not returned — but `Detach` at `:70-79` has the same hole, so `NewWindowRequested` can fire twice and open a customer link in two tabs). Note `MASTER-PLAN.md:§10` still lists "orphan `NavigationCompleted` handler" as an open RED item. | Key all four on the `instanceId` string, or hang the state off `SessionEntry` (`InstanceSessionManager.cs:831`). | M | med |
| **S2-04** | `TriagePersistenceService` was never migrated to `CorruptFileRecovery`; mark-handled/snooze state can be lost with no message and, in Release, no log. | `Services/TriagePersistenceService.cs:86-91` catches only `JsonException`, discards `BackupCorruptFile()`'s return value, and logs via `Debug.WriteLine`. An `IOException` (locked by antivirus/OneDrive) escapes `LoadAsync` entirely. Contrast `Services/CorruptFileRecovery.cs:32` and `Services/AppSettingsService.cs:71`, and the refuse-to-write guard at `Services/InstanceRegistryService.cs:723-729`. | Route through `CorruptFileRecovery`; refuse to save when preserve failed. | S | low |
| **S2-05** | Backups capture stale data — no flush precedes the zip, and only top-level `*.json` plus `avatars/` are included. | `Services/LocalBackupService.cs:52-73`, `:139-166` (`SearchOption.TopDirectoryOnly`). Debounced stores such as `TriagePersistenceService.cs:240-270` have unwritten state at backup time. | `await FlushPersistentStateAsync()` first; fail loudly if `LastFlushFailures` is non-empty. | S | low |
| **S2-06** | "Instance" — the app's internal noun — is spoken and displayed where the visible product says "account". | Visible title **and** accessible name: `Dialogs/DeleteInstanceDialog.xaml:6,10`, `RenameInstanceDialog.xaml:6,11`, `EditInstanceMetadataDialog.xaml:6,11`, `SetLocationDialog.xaml:6`. Accessible name only: `Pages/SettingsPage.xaml.cs:52,62,63,64,65,83,84`. Visible copy: `Pages/SettingsPage.xaml:121,142,372,385,389,402,406,419,423,436,440,652,657,679,899`, `Controls/CommandPalette.xaml:36`. Context menu: `Services/Shell/ShellController.cs:475,484,490,495,500,505,525,532,540`. The correct wording already exists at `Controls/WorkspaceSidebar.xaml:47,52,59` and `Dialogs/AddInstanceDialog.xaml:11`. | Rename every user-facing literal to "account"; keep `x:Name`/`AutomationId`. Add a source-scan test with `Ollama instance` (`SettingsPage.xaml:468`) as the sole exception. | M | low |
| **S2-07** | Dialog tab order is decided by `GetHashCode()` and therefore changes between runs. | `Services/FocusTrapHelper.cs:105-106` — `.OrderBy(TabIndex).ThenBy(GetHashCode())`. None of the five consumers sets `TabIndex`, so WinUI's `int.MaxValue` default makes every control tie and the hash decides. Applied at `Dialogs/AddInstanceDialog.xaml.cs:60`, `DeleteInstanceDialog.xaml.cs:27`, `EditInstanceMetadataDialog.xaml.cs:53`, `RenameInstanceDialog.xaml.cs:27`, `Controls/CommandPalette.xaml.cs:41`. The handler registers with `handledEventsToo: true` and sets `e.Handled` (`:22,89`), so it overrides `ContentDialog`'s own correct loop. | Drop the helper from the four `ContentDialog`s (they already contain focus); keep it for `CommandPalette` and sort by visual-tree order. | S | low |
| **S2-08** | Every Re-sync prints developer jargon and raw exception text into the dashboard's attention banner. | `Controls/CommandCenterPanel.xaml.cs:3603` `AttentionText.Text = "Probe · " + …`, fed by `:3586` `$"{instance.DisplayName}: {ex.Message}"`. Renders e.g. `Probe · DHA-2: Object reference not set to an instance of an object.` | Drop the prefix; one owner-readable sentence per account; log the exception. | S | low |
| **S2-09** | Save/Export fails silently on four surfaces. | `Pages/ReportsPage.xaml.cs:98-101` (.md), `:117-119` (.csv), `Pages/AnalyticsPage.xaml.cs:236-238`, `Dialogs/WeeklyReportDialog.cs:281-284`. Full or read-only disk → nothing happens, no file, no message. | Surface a short failure notice — the pattern `Pages/SettingsPage.Data.partial.cs:96` already uses. | S | low |
| **S2-10** | Analytics and Reports have no no-accounts empty state; a fresh install is told activity "looks steady". | Analytics: `Pages/AnalyticsPage.xaml.cs:60-96` has no zero-account branch — a KPI row of zeros and five separately-empty cards. Reports: `Pages/ReportsPage.xaml.cs:62-71` always calls `WeeklyReportDialog.Populate`, whose only empty branch is `Dialogs/WeeklyReportDialog.cs:147-155` *"Nothing notable in this period — activity looks steady."* Reviews has a state (`Pages/ReviewsPage.xaml:21-40`) but no way forward from it. | One shared no-accounts `EmptyStateView` with an "Add account" action on all three. | M | low |
| **S2-11** | Per-row buttons in the Settings account lists have no distinguishing accessible name — on an irreversible action. | `Pages/SettingsPage.xaml:804-809` ("Change icon…"), `:868-871` ("Restore"), `:872-875` ("Delete permanently"). Narrator reads "Restore button, Delete permanently button, Restore button…". `Controls/NotificationFeedPanel.xaml:130` already does this correctly. | Bind `AutomationProperties.Name` to `$"{DisplayName}: delete permanently"`. | S | low |
| **S2-12** | Three `async void` UI handlers are unguarded process-crash vectors (`App.xaml.cs:90` leaves `Handled = false`). | `MainWindow.xaml.cs:149` (`NavAddressBox_KeyDown`), `:170` (`NavSaveSiteButton_Click` → `SaveCurrentSiteAsInstanceAsync`, which can throw `InvalidOperationException(RefusedSaveMessage)` from `InstanceRegistryService.cs:729` — a documented reachable state), `:300` (`RequestTrayQuit`). `:676` is guarded. | try/catch → `ShowErrorAsync` in each body. | S | low |
| **S2-13** | The KPI sparkline history is corrupted by viewing a past date range. | `Controls/CommandCenterPanel.xaml.cs:2066-2068` calls `KpiTrendStore.Record` with the **range-scoped** `overallPct` (the method takes `rangeStart`/`rangeEnd`) but the **unscoped** `awaitingSplit.NeedsReply`; `Services/Oversight/KpiTrendStore.cs:52` keys the write by *today* and overwrites. Selecting "last 30 days" therefore writes a 30-day average into today's history slot, permanently. | Record only when the range is Today, or key the store by the range as well. | S | low |
| **S2-14** | The Analytics SLA donut relabels unread-derived figures as SLA outcomes, and sits beside range-scoped tiles while itself being unscoped. | Slices are reconstructed by multiplying `MeasuredCount` by an already-rounded `OnTimePercent` (`Services/Analytics/ChartSeriesBuilder.cs:111-114`); under the chat-snapshot path `OnTimePercent` is the unread proxy (`Services/Oversight/OversightRollupBuilder.cs:160`), yet the slices are labelled "SLA met"/"SLA missed" (`Pages/AnalyticsPage.xaml.cs:211-212`). Separately, the donut and the Account Leaderboard are built from `OversightService.BuildSnapshot(...)` with no window (`Services/Analytics/AnalyticsPagePresenter.cs:84-87,114`) while every KPI tile on the page is scoped to `rangeDays` (`:57`). | Relabel to what it measures; scope it to the range or state its basis on screen. | M | med |
| **S2-15** | The weekly report states one number under two incompatible nouns, and prints an all-time figure inside a period document. | `Services/Analytics/BusinessReport.cs:257` "(N replies measured)" and `:260` "Replied to **N** waiting customers this week" are both fed `thisPeriod.SampleCount` (`Services/Analytics/DashboardReportHelper.cs:78`) — FRT samples, not distinct customers. `BusinessReport.cs:272` "Busiest: Tuesday, around 2 PM" comes from `GetBusiestWindow` called with `null, null` (`DashboardReportHelper.cs:33` → `MessageAnalyticsService.cs:1310-1311`). | Count distinct conversations for the customer sentence; pass the range to `GetBusiestWindow`. | S | low |
| **S2-16** | On the IndexedDB fallback path, already-answered inbound calls are still queued as "missed · Call back". *(Confirms open item D2.)* | `lastCallOutcome` is emitted only by `Assets/Scripts/whatsapp-store-bridge.js:741`; the IndexedDB scan has no producer. `Services/Oversight/ReplyNeed.cs:345` documents that an empty outcome keeps the call counted, reached at `:384-386`. Correct-by-design (unknown must not close a chat) but the owner is never told which path an account is on — this is F-SNAP-02. | Surface the read path per account (Settings → Data already has a "Store bridge" health line); caveat the missed-call figure when an account is on the fallback. | M | low |
| **S2-17** | Releases do not gate on the UI smoke tests. | `.github/workflows/build.yml:287` — `release` declares `needs: package`, not `needs: ui-smoke`; `ui-smoke` (`:255`) also depends on `package` and runs alongside. It can be red on every tag and the release still publishes. | Either gate on it or delete the implication that it gates — see §3 doc corrections. | S | low |
| **S2-18** | Documentation lane — 14 further S2 doc defects. | See §3. | — | M | low |

### S3 — friction, inconsistency, workable but wrong

| # | Finding | Evidence | Fix | Effort | Risk |
|---|---|---|---|---|---|
| S3-01 | Two full status palettes ship side by side: `SystemFillColor*` (Windows-owned) and the audited `UmStatus*`. Two greens, two ambers, two reds. **All measured pairings pass AA** (success 5.44/7.67, caution 5.31/10.60, critical 5.69/6.89, attention 6.35/7.78 against the documented light/dark card surfaces) — so U6 is a *consistency* defect, not a contrast one. Worst instance: the sidebar **selection** indicator is painted with the *success* token. | `Controls/CommandCenterPanel.xaml.cs:1791,1198,1906,2232,2491,729,830,2032,2716,2767,3082,2887`; `Controls/CommandCenterPanel.xaml:191,329,342,368,383,422,436,471,480,509,513,517`; `Controls/WorkspaceSidebar.xaml.cs:1094,1107`; `Dialogs/AccountDetailDialog.cs:72,73,179`; `Dialogs/WeeklyReportDialog.cs:131,176-178`; `Pages/AboutPage.xaml:109,121,133`; `Controls/Charts/KpiStatCard.cs:55,103` | Route through `UmSemanticBrushes`; give selection its own token; extend `StatusContrastTests` to fail new `SystemFillColor*` references. | M | low |
| S3-02 | `ShowAlertToast` lacks the `_registered` guard its sibling has, so it cannot distinguish "toasts off" from "toasts broken"; `Initialize` also subscribes before `Register()` and never unsubscribes on the failure path. | `Services/Notifications/AppNotificationService.cs:64-125` vs `:129`; `:39-40`; `:54` | Add the guard and an `IsAvailable` the Settings page renders. | S | low |
| S3-03 | `IsRecognisedBackup` swallows everything, so a locked or permission-denied genuine backup is reported as corrupt — the owner may delete it. | `Services/LocalBackupService.cs:78-88`, consumed `:99` | Let `IOException`/`UnauthorizedAccessException` through with their own message. | S | low |
| S3-04 | `DashboardRefreshCoordinator` drives a `DispatcherQueueTimer` from arbitrary threads and lazily creates it unsynchronised. | `Services/DashboardRefreshCoordinator.cs:99-106`, `:92`, `:93`; sources fire from background flushes (`Services/ThreadRegistryService.cs:571,694`) | Enqueue the whole body; create the timer once in `Attach`. | S | low |
| S3-05 | Sidebar location-group headers are focusable but never announce expanded/collapsed. | `Controls/WorkspaceSidebar.xaml.cs:639-649` (a `Border` with `IsTabStop`), `:651-659`, `:661-662` (name is a constant) | Expose an `ExpandCollapse` peer or use a `ToggleButton`. | S | low |
| S3-06 | `DeltaBadge` encodes good-vs-bad in colour alone; the arrow only encodes direction. | `Controls/Charts/DeltaBadge.cs:62,66-74,77`; the class doc at `:9-13` says so | Append "improved"/"worse" to the automation name and add a glyph cue. | S | low |
| S3-07 | Two Analytics charts are silent to a screen reader, and the control built to fix that is dead. | `Controls/Charts/AreaLineChartView.cs:43` sets `IsTabStop = false` and never calls `SetName`, unlike `BarChartView.cs:154` / `DonutChartView.cs:110`; consumed at `Pages/AnalyticsPage.xaml:96,110`. `Controls/Shared/AccessibleChartHost.xaml.cs:7` has no instantiation outside `obj/`. | Give `AreaLineChartView` a `SetName` summary; delete `AccessibleChartHost`. | S | low |
| S3-08 | Re-sync with no professional accounts does nothing at all — no message, no state change, button stays enabled. | `Controls/CommandCenterPanel.xaml.cs:3542-3546` | Disable the button or show the empty-state banner. | S | low |
| S3-09 | The on-device-AI invariant is **defaulted, not enforced**: `OllamaEndpoint` is persisted with no loopback validation. The UI path is closed (`Pages/SettingsPage.xaml:478` is `IsReadOnly="True"`), so this is defence-in-depth, not a live breach — but a hand-edited settings file would send customer names and 800-char message bodies to a remote host while `SettingsPage.xaml:468` still reads "Nothing leaves your PC". | `Models/AppSettings.cs:291-301` normalises only; zero `Uri.IsLoopback` checks exist anywhere in the tree | Reject non-loopback in `AppSettings.Normalize`, falling back to the default. Three lines. | S | low |
| S3-10 | `Controls/ReviewHealthPanel.xaml{,.cs}` (541 lines) is unreachable, and live code still tunes itself around a handler that no longer runs. | Only definition at `Controls/ReviewHealthPanel.xaml.cs:16,30`; `Pages/ReviewsPage.xaml:17` hosts `ReviewDesk`. `Services/Oversight/GoogleReviewSnapshotService.cs:1003,1032` still reasons about "ReviewHealthPanel kicks off a scrape from its Loaded handler". | Delete both files; re-derive the throttle rationale. | S | low |
| S3-11 | 7 of 12 `AccessibilityTabOrderHelper` constants are never applied, and the sidebar footer hardcodes indices that disagree with them. | Never applied: `Services/AccessibilityTabOrderHelper.cs:9,11,13,15,19,21,23`. Drift: `Controls/WorkspaceSidebar.xaml:48,70,108` use 90/92/93 where the constants say 90/91/92. No helper call at all in `Pages/AnalyticsPage.xaml.cs`, `ReviewsPage.xaml.cs`, `ReportsPage.xaml.cs`, or any of the 14 `Dialogs/` files. | Apply the constants or delete the unused ones; make the footer read them. | S | low |
| S3-12 | Review coverage can never say "covers all" because the caller discards the fact the scrape recorded. *(Related to open item D5.)* | `Controls/ReviewDesk.xaml.cs:221` calls the 2-arg overload, dropping `ReachedLastPage` (`Services/Oversight/ReviewCoverage.cs:33`); `MaxPages = 1` at `Services/Oversight/GoogleReviewSnapshotService.cs:976` | Pass `ReachedLastPage` through. The pagination cap itself stays off until two consecutive passes agree on totals. | S | low |
| S3-13 | "Answered today" silently reads zero when a date range ends before now — the chip is labelled "today" but sits behind the range filter. | `Services/Oversight/ResponseTimeTracker.cs:202`, guards at `:186,191`; rendered `Controls/CommandCenterPanel.xaml.cs:3103-3109` | Compute it outside the window, or label it "answered in range". | S | low |
| S3-14 | KPI sparklines omit days with no reading, so points weeks apart plot as adjacent with no indication. | `Services/Oversight/KpiTrendStore.cs:97-100` | Zero-fill or mark gaps. | S | low |
| S3-15 | The Notification Hub empty state uses internal wording ("intercepted"). | `Controls/NotificationFeedPanel.xaml:219-224` | Reword. | S | low |

### S4 — polish

| # | Finding | Evidence |
|---|---|---|
| S4-01 | `ReviewAskCandidates.cs:128` stores a possibly-null `ConversationKey` into a non-nullable record field — the only live build warning (CS8604). `AskKey` (`:19`) happens to be safe today because `:119-123` guarantees one of the two is non-empty, so this is latent, not live. |
| S4-02 | `StarsFor` rounds away from zero (`Controls/ReviewDesk.xaml.cs:394`), so 4.5–4.99 renders five filled stars directly beneath the number "4.6". |
| S4-03 | `KpiTrendStore` uses raw `DateTime.Now` (`:52,76,92`) rather than the injectable `LocalDayBoundary` its peers use — same result here, untestable against another zone. |
| S4-04 | The idle-reaper `Timer` is never disposed and its work is unobserved. `Services/Session/InstanceSessionManager.cs:55`. |
| S4-05 | Absolute developer paths leak into user-visible stack traces in `app.log` (`…\.claude\worktrees\project-skills-plugins-cf86f5\…`). Fix with `PathMap`/`DeterministicSourcePaths`. |
| S4-06 | Ellipsis style inconsistent: `Pages/SettingsPage.xaml:808` uses `...`, `Dialogs/DeleteInstanceDialog.xaml:24` uses `…`. |
| S4-07 | `PlatformModuleSettingsHelper.GetSelectablePlatforms` takes an `AppSettings` it never reads (`:56-57`); `NormalizePlatformModules` (`:59-62`) only null-checks and does nothing. |
| S4-08 | `THIRD-PARTY-NOTICES.md` omits the test-only packages (`coverlet.collector`, `Microsoft.NET.Test.Sdk`, `xunit.runner.visualstudio`, `Xunit.SkippableFact`). |
| S4-09 | `docs/design-system/contrast-audit.md:154` says `StatusCueTests` has 6 tests; the file has 10. |
| S4-10 | 22 MB of ONNX runtime ships in a 214 MB payload for Tier-1 AI, which is unimplemented. It is a declared transitive dependency (`UnifiedMessenger.deps.json`, via the Windows AI runtime assets), not a stray file — so removing it means an `ExcludeAssets`, not a cleanup. |

---

## 2. Data-accuracy ledger

**No account was logged in for the metric trace, so no live value was checked against reality.** Every classification below is from source. The one live observation available — 894 conversation rows in `app.log` — is what produced S1-07.

| Metric (as the owner sees it) | Source | Class | Honest? | Verified |
|---|---|---|---|---|
| Caught up % (KPI + per-account) | `OversightRollupBuilder.cs:160` → `MetricMath.cs:52`; render `CommandCenterPanel.xaml.cs:2043-2050` | derived (unread proxy) | Yes — tooltip at `:2083` says "not reply speed"; 100-boundary re-guarded at `:2050` | static |
| Needs a reply / Backlog / hero | `OversightChatSnapshotService.cs:367-429`; render `:2060,2092,2196` | measured | Yes — same `IsEffectivelyAwaiting` predicate (`:305-308`) feeds both, so "100 % caught up" cannot coexist with a queue | static |
| Median first response time | `ResponseTimeTracker.cs:216,334-344` | measured (forward-tracked, sparse by design) | Yes — sample count rendered at `:2140` | static |
| SLA met % (dashboard + Analytics KPI card) | `ResponseTimeTracker.cs:222`; `AnalyticsPagePresenter.cs:105` | derived | Yes | static |
| **SLA met % (Analytics donut centre)** | `AnalyticsPage.xaml.cs:205-206` | estimated | **No — S1-06.** Different denominator, identical label, plain `Math.Round` | static |
| **SLA met/missed/No-SLA slices** | `ChartSeriesBuilder.cs:111-114` | estimated | **No — S2-14.** Unread-derived, relabelled as SLA | static |
| Answered today | `ResponseTimeTracker.cs:202` | measured | **Partly — S3-13.** Range-scoped behind a "today" label | static |
| Messages/day, Messages over time, Busiest window | `MessageAnalyticsService.cs:1161-1163`, buckets `:251-258` | measured | Yes — local-day keyed throughout; empty set → `HasData=false` at `:1156` | static |
| **Missed calls** | `ReplyNeed.cs:374-386`; outcome only from `whatsapp-store-bridge.js:741` | measured on bridge, **degraded on IndexedDB** | **No on the fallback — S2-16** | static |
| KPI delta badges | `ChartSeriesBuilder.cs:20-42` | derived | Yes — zero baseline → `MetricDelta.None` at `:22-26` | static |
| **KPI sparklines** | `KpiTrendStore.cs:50-55,90-104`; written `CommandCenterPanel.xaml.cs:2066-2068` | derived | **No — S2-13 (scope), S3-14 (gaps)** | static |
| End-of-day projection | `MessageAnalyticsService.cs:1278-1304` | estimated | Yes — suppressed below 5 % elapsed (`:1298`); DST skew bounded and documented (`:1266-1276`) | static |
| Review coverage line | `ReviewCoverage.cs:69-94`; render `ReviewDesk.xaml.cs:217-221` | measured + caveat | Yes — the caveat is rendered, not just commented | static |
| **Unanswered / Needs a reply (Reviews)** | `ReviewDesk.xaml.cs:339,491` ← `_queue` | **truncated sample shown as a total** | **No — S1-05** | static |
| Reply rate % (Reviews) | `ReviewDesk.xaml.cs:480`; basis `:547` | derived | Yes — "N of M read" rendered | static |
| Google star rating + lifetime total | `GoogleReviewSnapshotService.cs:227-268`, ≤6 h cache (`:445`) | measured (scraped) | Yes — freshness shown at `ReviewDesk.xaml.cs:210-213` | static |
| Weighted business rating | `ReviewDesk.xaml.cs:363-390` | derived | Yes — labelled "weighted across N locations"; null unless every location has both (`:377-383`). Star glyphs round up (S4-02) | static |
| Review velocity / rating trend / quietest branch | `ReviewTrend.cs:48-113`; render `ReviewDesk.xaml.cs:296-316,508-517` | derived | Yes — each needs two readings; span rendered via `SpanLabel` (`:149-156`); `HistoryPending` otherwise | static |
| Per-review star rating | `GoogleReviewSnapshotService.cs:111-118` (leading run of colour) | estimated (CSS heuristic) | Yes — unreadable → `Unrated`, labelled "Rating unread" (`ReviewQueue.cs:63-76`) | static |
| Google median reply time | `ReviewDesk.xaml.cs:513` — hard-coded "—" | not computed | Yes — states why. **Confirms open item D6: unobtainable, correctly disclosed** | static |
| **Weekly report: replies / customers / busiest** | `BusinessReport.cs:257,260,272` | derived | **No — S2-15** | static |
| Weekly report: return rate, busiest-account share | `BusinessReport.cs:171,187` | derived | Yes — `HonestPercent` with guarded denominators | static |

**Sweeps.** All ten `MetricMath.HonestPercent` call sites guard `total > 0` first, so the `total <= 0 → 100` branch (`MetricMath.cs:35-40`) is unreachable from any render path. Three percentages bypass `HonestPercent`: `AnalyticsPage.xaml.cs:206` (live — S1-06) and `DashboardPageHelper.cs:91` / `MessageAnalyticsService.cs:1609-1610` (both currently unreachable dead code — latent). Day bucketing is local throughout (`LocalDayBoundary`), with no UTC-keyed bucket anywhere in scope.

---

## 3. Doc corrections

Exact stale text → exact replacement.

| # | File:line | Stale | Replacement |
|---|---|---|---|
| D-01 | `README.md:78` | "Prompts contain **aggregate counts only** — never customer names or message text." | "Insight-strip prompts contain aggregate counts only. Triage prompts include the customer name and up to 800 characters of the message — sent to your local Ollama instance only, never off-box." |
| D-02 | `README.md:186` | `& "$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe" installer.iss` | `& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss` |
| D-03 | `README.md:221` | "See the repository license file for terms." | Add a LICENSE file, or: "This project has no published license file yet; all rights reserved." (`git ls-files` finds none.) |
| D-04 | `AGENTS.md:80` | "Version sync — **4** files" | "Version sync — **5** files" (the table beneath lists five, plus `phase-status.md` at `:92`) |
| D-05 | `AGENTS.md:222` | "`OnResyncHistory` reloads each account's WebView before probing" | "`CommandCenterPanel.RunResyncAsync` reloads each account's WebView before probing" (`OnResyncHistory` exists nowhere; the handler is `OnResyncClick` → `RunResyncAsync`, `Controls/CommandCenterPanel.xaml.cs:3472,3535`, and it does still reload at `:3576`) |
| D-06 | `AGENTS.md:313` | "Test filter too broad → hangs headless \| Use exact class names in `--filter`" | "Test filter too broad grabs unintended classes \| Filters are for iteration only; run the full suite before pushing." — the gotcha table still asserts the rule that `AGENTS.md:111-114` retracts, and `docs/ui-vision-implementation-prompt.md:218` cites it as authority. |
| D-07 | `AGENTS.md:420` | "Optional follow-ups … business-hours-aware FRT, AI-narrated report headline … PNG/PDF export" | All three shipped: `Services/Oversight/BusinessHoursCalculator.cs`, `Controls/CommandCenterPanel.xaml.cs:3490-3507`, `Dialogs/WeeklyReportDialog.cs:72`. Leave only "OS-scheduled report, PDF export, empty-state sweep". |
| D-08 | `docs/phase-status.md:113` | "**default cap = 0 (unbounded)**" | "default cap = 6" — `Services/AppSettingsService.cs:158`; the same file contradicts itself at `:174,193`, and `AGENTS.md:264` says 6. Mark the row ✅. |
| D-09 | `docs/phase-status.md:138` | "Generic-URL webview instances \| ☐ \| `PlatformDefinition` is WhatsApp-only" | "✅ v4.19.0 — `generic` platform + `AllowsCustomUrl` (ADR-007)". Nine platforms are registered. |
| D-10 | `docs/phase-status.md:166` | "Telegram … and Messenger … are registered platforms — selectable in 'Add account'" | Telegram is in `HiddenFromPicker` (`Services/PlatformModuleSettingsHelper.cs:53-54`) and is **not** offered. |
| D-11 | `docs/phase-status.md:89,185` | "the new channels/features … are mostly ☐. No phase is 'complete.'" | Prefix both blocks "**Historical (pre-v4.20) — superseded by the tables below.**" The same file marks Phase 1 complete (`:189`) and Phase 4 done (`:141`). |
| D-12 | `docs/phase-status.md:100`, `docs/architecture/system-map.md:50,63` | `OperationsCommandCenterService`, `OperationsCommandCenterViewModel`, `BranchWorkspacePillBarViewModel`, `OccSnapshotPresenter`, `OccThreadCardPresenter` | None of these five types exist. Replace with `CommandCenterPanel` / `OversightService` / `OversightRollupBuilder`; the OCC was retired v4.27.0 (`phase-status.md:72`). |
| D-13 | `docs/architecture/system-map.md:3,72,84` | "updated v3.2.0" · "unit tests (442)" · "WhatsApp and WhatsApp Business Web only" | Shipping version is 4.99.45; the suite is **1744**; six platforms are offered. The document predates the Oversight engine, section nav, and the Reviews/Analytics/Reports pages. |
| D-14 | `docs/architecture/adr/occ-grid-layout.md:14,21,22` | `RichTriageStore`, `OccLayoutGridApplier`, "`AppSettings` version bumped to 5", "Status: Accepted" | None of those types exist; `Models/AppSettings.cs:6` is `CurrentVersion = 19`. Change status to "**Superseded (v4.27.0)**". |
| D-15 | `docs/architecture/adr/001-mvvm-migration.md:20`, `adr/README.md` | "`Controls/Occ/` helpers"; index lists only ADRs 001–004 | No `Controls/Occ/` exists; ADRs 005–009 and `occ-grid-layout.md` are missing from the index. |
| D-16 | `docs/design-system/README.md:23,25-27` | "`OperationsThreadCardView`" · "## OCC layout" | The type does not exist; OCC retired v4.27.0. `Controls/Shared/` holds eight controls, none named that. The doc also omits `Controls/Charts/`. |
| D-17 | `docs/design/review-desk-spec.md` header | State columns describe an unbuilt feature | Add "**Implemented v4.99.40 → v4.99.43.** State columns are historical; current state is `remaining-work.md §0.2b`." Everything the spec lists as "new"/"needs Ollama"/"does not exist" now ships. |
| D-18 | `docs/ux-modernization-plan.md:68-73` | "`Tokens.xaml` declares flat `<Color>` keys with **no `ThemeDictionaries`**" | `Themes/Tokens.xaml:10-143` *is* wrapped in `ThemeDictionaries`. Mark issue D resolved; banner the doc as baseline v4.56.0. |
| D-19 | `docs/ui-vision-implementation-prompt.md:47` | "Google's embedded surface doesn't expose rating/total" | It does — `GoogleReviewSnapshotService.ProfileRating`. `AGENTS.md:233-237,321` says so and warns against re-deriving it. |
| D-20 | `docs/ui-vision-implementation-prompt.md:76,84,218` | "4-file version sync" · "8 partial classes" · "headless-hang rule" | 5 files; **9** `SettingsPage.*.partial.cs` files; the rule was retracted. Add a status banner (WP0/1/3/5/6 shipped). |
| D-21 | `AGENTS.md` build section | — (missing) | Add: `dotnet build` at the repo root fails (`NETSDK1032`); build the **csproj**, or fix the `.sln` mapping (S2-01). |
| D-22 | `AGENTS.md` CI / `docs/validation/completion-criteria.md:17` | "534 unit tests (v4.2.0)"; implication that UI smoke gates releases | 1744 tests. `build.yml:287` shows `release` needs `package`, not `ui-smoke`. The staleness check at `build.yml:64-73` fires a `Write-Warning` on every run and is structurally ignored. |

**Verified accurate, no action:** `docs/design-system/scales.md` (matches `Themes/Tokens.xaml:179-196` exactly), `docs/design-system/contrast-audit.md` (bar S4-09), ADRs 005–008, `THIRD-PARTY-NOTICES.md` for runtime packages, and every doc under `docs/validation/` plus the two superseded research docs — all correctly banner themselves as historical. Every class and file path named in `AGENTS.md` exists except `OnResyncHistory`.

---

## 4. Improvements — each with the YAGNI test applied

The question for each is: **what breaks if we don't?**

### Worth doing

| Improvement | What breaks without it |
|---|---|
| **Make the store-bridge-vs-IndexedDB read path visible per account** (F-SNAP-02, S2-16) | The missed-call figure is wrong on the fallback and nothing on screen says which path an account is on. This is the one open finding that changes a number the owner acts on. |
| **One `umTruncate` helper shared by both JS producers and `TranscriptBuilder`** (S1-07) | A customer stays invisible. One helper, six call sites — the root-cause fix is a smaller diff than guarding the parser. |
| **A source-scan guard test for "instance" in user-facing strings** (S2-06) | The rename regresses the first time someone adds a dialog. `DesignScaleTests` already proves this pattern works here. |
| **`Uri.IsLoopback` guard on `OllamaEndpoint`** (S3-09) | Nothing today — the UI box is read-only. Worth it anyway because it is three lines and it converts the project's hardest constraint from "defaulted" to "enforced". |
| **Delete `ReviewHealthPanel` and `AccessibleChartHost`** (S3-10, S3-07) | 700-odd lines of dead code, and live throttling logic in `GoogleReviewSnapshotService` still reasons about a handler that no longer runs. Deletion over addition. |

### Killed — my own ideas that do not survive the test

| Idea | Why it dies |
|---|---|
| Re-enable Google review pagination | It was turned off for a measured reason (2–3× over-count). The desk already states its own coverage. Nothing breaks while it stays off. Leave `MaxPages = 1`; ship S3-12 (pass `ReachedLastPage` through) instead — one argument, not a pagination project. |
| Add a metrics abstraction / strategy layer so donut and KPI share a definition | One interface, one implementation, to fix two mislabelled strings. Fix the labels (S1-06, S2-14). |
| Strip the 22 MB ONNX runtime | It is a declared transitive dependency, not a stray. Removing it is an `ExcludeAssets` gamble against a Windows AI runtime asset, to save 10 % of an installer nobody has complained about. Revisit only if Tier-1 is formally dropped. |
| Build a JS test harness (Jint/ClearScript) for the five scrapers | Real gap, genuinely large effort, and it competes with eight S1s. Worth proposing *after* the S1s — not now. Recorded in §6 as a later phase, not killed outright. |
| Fix all 63 remaining `async void` sites | Only three are reachable crash vectors (S2-12). Fix those three. |
| Raise the CI coverage floor | Measured 37.9 % vs a 25 % floor. Raising it is a number-moving exercise that gates nothing real; the actual gap is *which* code is untested (§6 phase 4), not the percentage. |
| Rewrite `docs/architecture/system-map.md` | It is wrong (D-13) but nothing reads it to make a decision. Banner it as stale; rewrite only if someone needs it. |

---

## 5. Unverified

Everything here is stated as unknown, not assumed good.

1. **Live metric accuracy — the whole of §2.** No account was logged in during the trace, so not one displayed figure was checked against reality. Settled by: log in one WhatsApp account and one Google Business account, then hand-compute three metrics.
2. **Whether `AppNotificationManager.Show()` throws or silently no-ops when `Register()` failed.** Proving it requires actually calling `Register()`, which writes an HKCU CLSID and a Start Menu shortcut — a state change I did not make. S1-01 does not depend on it: `ShowInfoToast` is hard-gated on `_registered` at `AppNotificationService.cs:129`, covering three of the four call sites.
3. **The shutdown deadlock (S1-03) was traced, not reproduced.** It only bites when an await in `ShutdownAsync` genuinely yields; if the flush completes synchronously the whole chain stays on the UI thread and exits cleanly. That is why it is intermittent. Settled by: close via X with `RunInBackgroundOnClose` off, ten times, watching for a surviving process.
4. **The `ConditionalWeakTable` failure (S2-03) is GC-timing dependent** and has never been reproduced on demand. The mechanism is documented in `AGENTS.md`; the four sites are confirmed present.
5. **High-contrast behaviour of every `SystemFillColor*` site.** `contrast-audit.md` records that HC was checked for the brand token only. Settled by running under the four Windows HC themes.
6. **Two alpha-composited system brushes** (`SystemFillColorAttentionBackgroundBrush`, `SystemFillColorNeutralBackgroundBrush` — `CommandCenterPanel.xaml:329,422`, `:1794`, `KpiStatCard.cs:55`) have no fixed ratio; effective colour depends on the parent surface. Settled by sampling rendered pixels.
7. **No real screen reader has been run.** The UIA tree was read statically; nobody has listened to it.
8. **Whether the idle reaper can close a WebView2 mid-scan.** `IsReapEligible` (`InstanceSessionManager.cs:576-591`) exempts professional accounts, but a `googlebusiness` account categorised Personal could in principle be reaped under an in-flight scrape. Settled by confirming whether a `googlebusiness` account can be Personal.
9. **Whether the nightly UI smoke workflow is in fact red.** The YAML and `UnifiedMessenger.UiSmokeTests/Program.cs:88-124` establish that it *would* be (exit 4 for both "no desktop" and "app failed to launch", no `continue-on-error`); run history is not readable from the filesystem.
10. **Coverage is 37.9 % as of a 2026-06-11 report**, not measured today.
11. **`review-desk-spec.md:20` says 1,671 total (992/435/244); `AGENTS.md:242` says (991).** A one-review discrepancy between two docs, unresolvable without a live scrape.
12. **ARM64 is published and checksum-verified but has never been installed or run.** Unchanged from `remaining-work.md §0.4`.
13. **Uninstall data-erasure, the unhappy-review toast firing path, the "ask for a review" panel with a live candidate, and the all-caught-up hero** remain unobserved — all four need live data or destructive actions. Note that S1-01 means the toast path cannot fire *at all* until notifications are fixed, which supersedes the "never fired" entry in `§0.4`.

---

## 6. Proposed order of work

Dependency-aware. **The first three are ready to start now.**

### Phase 1 — stop the bleeding (the first three)

1. **S1-04 · theme-change crash.** Marshal `ThemeService.OnSystemColorValuesChanged` (and the high-contrast watcher) onto the UI thread. Self-contained, one file, ~5 lines, log-confirmed repro. Nothing depends on it; do it first because it is the cheapest S1.
2. **S1-02 · backup restore silently reverted.** Suppress the shutdown flush and restart from the app. One file plus its caller. Must land **before** S1-03, because S1-03 changes the shutdown path both fixes touch.
3. **S1-07 · dropped conversation.** One `umTruncate` helper, six call sites, plus routing the skip count into the existing "unreadable chats" hint. Independent of everything else.

### Phase 2 — the rest of the S1s

4. **S1-03 · shutdown deadlock.** Restructure `OnMainWindowClosed` to the shape tray-quit already uses. Do after S1-02.
5. **S1-05 · Reviews unanswered count**, then **S1-06 · duplicate "SLA met"**, then **S2-14 · SLA donut labelling** — one metric-labelling pass across the two pages. S1-06 and S2-14 share a file; do them together.
6. **S1-01 · notifications.** Largest S1 and the only one with real design risk. Deliberately last in this phase: it needs an AUMID decision, an installer change, and a Settings health surface. Everything in `§0.4` about the unhappy-review toast is blocked behind it.
7. **S1-08 + D-01…D-03 · README truth pass.** Cheap; bundle with any of the above.

### Phase 3 — silent failure and honesty

8. **S2-02 · `Debug.WriteLine` → `AppLogger`** across all 33 sites. Do this early in the phase: it is what makes everything after it diagnosable.
9. **S2-04, S2-05, S3-03 · durability**: migrate `TriagePersistenceService` to `CorruptFileRecovery`, flush before backup, stop reporting locked backups as corrupt.
10. **S2-09, S2-08, S2-12 · surface the failures** the user currently cannot see.
11. **S2-16 / F-SNAP-02 · surface the read path** per account.
12. **S2-13, S2-15, S3-13, S3-14 · the remaining metric-honesty items.**

### Phase 4 — accessibility, consistency, and the guards that hold them

13. **S2-06 · "instance" → "account"** with its source-scan guard test.
14. **S2-07, S2-11, S3-05, S3-06, S3-07, S3-11 · the accessibility set.** One pass, one reviewer.
15. **S3-01 · one status palette**, with the `StatusContrastTests` extension that stops it re-drifting.
16. **S2-10 · the empty-state sweep** (closes `remaining-work.md` U8).
17. **Regression tests for every S1 fixed above**, then the highest-value untested-and-material gaps: `OversightService.BuildSnapshot`, `ConversationNoiseFilter` (9 public functions, zero tests), `GitHubUpdateService.ApplyUpdateAsync` (the one path that executes a binary), `TranscriptBuilder`'s 800-char bound, `LocalBackupService` partial-archive restore, and `InstanceSessionManager` disposal.

### Phase 5 — housekeeping, only once the above is green

18. **S2-01 · fix the `.sln` platform mapping.**
19. **S2-03 · re-key the four `ConditionalWeakTable`s** on `instanceId`. Deferred this far because it is unreproducible and the surrounding session code will have moved under phases 3–4.
20. **S3-10 · delete the dead controls.**
21. **The doc corrections in §3** as one commit. Do them last so they describe the tree as it then is, not as it is now.
22. **S2-17 · decide whether UI smoke gates releases**, and either wire it up or delete the implication.
23. **S4 items**, as budget allows.

### Explicitly not scheduled

The five items in `remaining-work.md §0.5` remain gated on external dependencies, and nothing found in this audit unblocks them. The three owner decisions in `§0.6` are unchanged — **the SLA-threshold decision remains the highest-value item on the whole list that costs no engineering time**, and it now matters more, because S1-06 means the SLA figure the owner would be reacting to is rendered two different ways on one screen.
