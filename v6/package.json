{
  "name": "unified-messenger-v6",
  "version": "0.0.0",
  "private": true,
  "description": "Unified Messenger v6: Electron + TypeScript rebuild. v5 (UnifiedMessenger/) keeps shipping until parity.",
  "type": "module",
  "engines": { "node": ">=24.12" },
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "22.20.2",
    "typescript": "7.0.2"
  }
}
